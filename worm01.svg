<svg xmlns='http://www.w3.org/2000/svg'  
  version="1.1"
  onload="startup(evt)" onmousedown="mouseDown(evt)"  
  onmousemove="mouseMove(evt)" onmouseup="mouseUp(evt)" 
  onresize="resize(evt)" onzoom="resize(evt)">   
  <!--
    Author: Jan van der Meiden
    jvdmeiden@gmail.com
    Version: 2012.01  Copyright 2012 Jan van der Meiden.
    Copying and distribution of this file, with or without modification,
    are permitted in any medium without royalty provided the copyright
    notice and this notice are preserved.
  -->     
  <script type="application/ecmascript"> 
    <![CDATA[
      var SVGDoc = null;
      var SVGRoot = null;
      var dotRoot = null;
      var linesRoot = null;
      var heads = null; 
      var svgns = 'http://www.w3.org/2000/svg';      

      // Variables 
      var sqrt3 = Math.sqrt(3);
      var wormType="12345-12345-12345-12345-12345-12345-1";     
      var bgColor="#2f2f2f";
      var gridStep=8;
      var dotColor="#4f2f2f";
      var dotSize=2;
      var dotTrans=0.75;
      
      // 'Global' objects
      var board = new Object;
     
      
      function startup(evt){ 

	resize(evt);
        console.log(wormType);
      } 
      
      function mouseDown(evt){ 
      } 
      function mouseUp(evt){ 
      } 
      function mouseMove(evt){ 
      } 
      function resize(evt){ 
        // set background and find boxsize
        var newRect = SVGDoc.createElementNS(svgns,"rect");
        newRect.setAttribute("x",0);
	newRect.setAttribute("y",0);
	newRect.setAttribute("width","100%");
	newRect.setAttribute("height","100%");
	newRect.setAttribute("fill",bgColor);
        dotRoot.appendChild(newRect);	
	var box = newRect.getBBox();
        board.x = box.width;
        board.y = box.height;

	console.log(board.x + "--" + board.y);	
	
	// draw a grid
	//

      } 
    ]]>
  </script>   
  
  <g id="Lines">  
  </g>  
  
  <g id="Dots">  
  </g>  
</svg>   